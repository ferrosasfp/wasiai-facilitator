# Validation Report — WKH-217 / HU-SOL-14 (Gasless Fee-Payer Sponsorship Solana)

**Veredicto**: APROBADO PARA DONE (HELD — no merge/deploy, opt-in-off, devnet-only)
**Fecha**: 2026-07-22
**Branch**: `feat/027-wkh-217-solana-feepayer-sponsorship` @ `6670fa9` (stacked sobre `feat/026-wkh-205-solana-adapter`, HELD)

## Runtime checks
- `npm run qa` (typecheck && lint --max-warnings 0 && format:check && test), ejecutado por mí en el branch actual: **933/933 tests OK, 66 test files OK**, cadena `&&` completa sin cortarse ⇒ typecheck/eslint/prettier también en 0. Coincide exacto con lo esperado por el orquestador (873 base + 50 nuevos + 10 del fix-pack anterior citados en contexto = 933).
- Env vars nuevas (`SOLANA_FEE_PAYER_PRIVATE_KEY`, `SOLANA_FEE_PAYER_SPONSOR_ENABLED`, `SOLANA_ESCROW_PROGRAM_ID`, `SOLANA_SPONSOR_MAX_COMPUTE_UNITS`, `SOLANA_SPONSOR_MAX_PRIORITY_FEE_MICROLAMPORTS`, `SOLANA_SPONSOR_MAX_FEE_LAMPORTS`, `SOLANA_SPONSOR_RATE_LIMIT_MAX/WINDOW_SEC`, `SOLANA_SPONSOR_DAILY_MAX_LAMPORTS`, `SOLANA_SPONSOR_MAX_REBROADCASTS`, `SOLANA_SPONSOR_POP_SECRET`): definidas en `src/infra/env.ts:198-231`, leídas 1:1 (mismo nombre) en `src/routes/solana-sponsor.ts:65-70,79,125-126`. Sin typos.
- Deploy target (Railway): NO VERIFICABLE / N/A — la HU queda HELD por diseño (opt-in-off, sin merge a `main`, sin activación en prod), por lo tanto no hay env parity que verificar contra un deployment target todavía. Esto es esperado, no un hallazgo.
- Migration/DB: N/A — esta HU no toca tablas nuevas (el contador diario vive en Redis, `src/core/solana-sponsor-cap.ts:25-28`, no en Postgres).

## ACs
| AC | Status | Evidencia |
|----|--------|-----------|
| AC-1 (sponsor camino feliz) | PASS | `src/__tests__/unit/solana-sponsor.cr1.test.ts:122-137` (T1/T1b, ok:true + fee upper bound); `src/__tests__/unit/solana-sponsor.route.test.ts:137-149` (T18, 200 + `{signature}`) |
| AC-2 (CR-1 rechazo tx no-esperada) | PASS | `cr1.ts:78-96` (Check 2, exactamente 1 business-ix + programId whitelisted); tests `solana-sponsor.cr1.test.ts:140-181` (T2/T3/T4a/T4b) — extra ix, programId random, tx vacía, solo ComputeBudget, todas `ok:false` |
| AC-3 (fee-payer nunca source/authority/signer) | PASS | `cr1.ts:175-186` (Check 5, escaneo de TODAS las ix); tests `cr1.test.ts:184-227` (★T5/T5b/T6/★CR-MNR-4) — SystemProgram.transfer, SPL transfer/setAuthority/close, fee-payer como `reference` no-sender, todas `FEE_PAYER_REFERENCED_IN_INSTRUCTION` |
| AC-4 (blockhash + ComputeBudget acotados) | PASS | ComputeBudget: `cr1.ts:98-131` + tests `cr1.test.ts:230-242` (T8a/T8b, `COMPUTE_UNITS_ABOVE_MAX`/`PRIORITY_FEE_ABOVE_MAX`). Blockhash: `broadcast.ts:187-197` (pre-sign) + `broadcast.test.ts:99-107` (★T11, `isBlockhashValid=false` → `SPONSOR_BROADCAST_EXPIRED`, `sendRawTransaction` NUNCA llamado) |
| AC-5 (concurrencia sin colisión) | PASS | `broadcast.ts:180-203` (`runExclusive(FEE_PAYER_SENTINEL_ID,...)`); test `broadcast.test.ts:110-131` (★T12, 5 tx concurrentes → 5 signatures únicas, `h.active.max === 1` confirma serialización FIFO sin interleave); rebroadcast/expiry: `broadcast.test.ts:134-159` (T13/T13b) |
| AC-6 (rate-limit + tope diario fail-closed) | PASS | Rate: `solana-sponsor-cap.ts:46-73` + tests `solana-sponsor.cap.test.ts:46-75` (T15a-d, Redis down/throw → `store_error_failclosed`). Daily: `solana-sponsor-cap.ts:84-118` + tests `cap.test.ts:83-126` (★T16b/c/d, over-cap y Redis-down → reject fail-closed, nunca fail-open) |
| AC-7 (KYC/PoP antes de parsear/firmar) | PASS | `routes/solana-sponsor.ts:118-121` (Step 2, PoP ANTES de rate-limit/CR-1/sign); `pop.ts:35-36` (fail-closed si secret unset); tests `route.test.ts:152-178` (T17a/★T17b — popProof ausente/inválido → 400/403, `h.cosignSpy` NUNCA invocado, confirmando que CR-1/sign no corren) |
| AC-8 (rent lo paga el sender, no el fee-payer) | PASS | Subsumido por Check 5 de `cr1.ts:175-186` (el fee-payer no puede ser `payer`/`funder` de ninguna cuenta nueva porque no puede aparecer en NINGUNA ix); test `cr1.test.ts:210-215` (★T7, fee-payer como `sender`/account[0] del deposit, que es el rent-payer real del escrow) → `FEE_PAYER_REFERENCED_IN_INSTRUCTION` |
| AC-9 (EVM byte-idéntico) | PASS | `git diff feat/026-wkh-205-solana-adapter...HEAD --name-only` (ejecutado por mí) — CERO archivos EVM tocados (`eip3009/*`, `wallet.ts`, `settle.ts`, `schemas.ts`, `ledger.ts`, `base-adapter.ts`, `solana-adapter.ts` no aparecen en el diff-stat); `app.ts`/`env.ts`/`logger.ts` verificados aditivos-only (`git diff` mostrado, solo `+`, cero `-`); `933/933` tests incluye la suite EVM preexistente sin cambio de assertion; test dedicado `solana-sponsor.env-optin.test.ts:122-131` (T21) confirma `/health` y `/supported` responden 200 con sponsorship OFF |
| AC-10 (key desde env, nunca logueada/hardcodeada) | PASS | `solana-fee-payer.ts:89` (`process.env.SOLANA_FEE_PAYER_PRIVATE_KEY`, nunca hardcode); `FeePayerKeyError` (líneas 46-51) lleva solo el NOMBRE del env, nunca el valor; redaction en `logger.ts:41-43` (`SOLANA_FEE_PAYER_PRIVATE_KEY`, `feePayerPrivateKey`, `SOLANA_SPONSOR_POP_SECRET`, `popProof`, `partialSignedTx`); test `route.test.ts:220-241` (★T19) fuerza un error y confirma que el JSON de 64 bytes de la key NUNCA aparece en logs (`CaptureStream`) ni en el body de la respuesta |

## Verificaciones críticas de seguridad
- **CR-1 anti-drain**: confirmado. El fee-payer nunca aparece en `keys` de ninguna ix (Check 5, `cr1.ts:175-186`) — vectores estrella T2/T5/T5b/T6/T7/CR-MNR-4 todos verdes, todos `ok:false` SIN firmar (`tx.partialSign` solo se alcanza en `broadcast.ts:200`, después de que `cosignAndBroadcast` recibe `v.ok===true` de CR-1 en el paso 2, `broadcast.ts:154-159`). `broadcast.test.ts:162-174` (★T14a) confirma explícitamente que un validator-reject NO llama a `sendRawTransaction` ni a `isBlockhashValid` — cero side-effects de red antes de la autorización.
- **fail-CLOSED del cap**: confirmado en dos capas — rate (`cap.test.ts:60-73`, ★T15c/d) y daily (`cap.test.ts:97-110`, ★T16c/d): Redis down o Redis-throw → `store_error_failclosed`, nunca `ok:true`. El CHECK del daily-cap (`onFeeEstimated`) sigue **pre-sign**: `broadcast.ts:166-177` corre ANTES del bloque `runExclusive` que firma (línea 180); `broadcast.test.ts:197-207` (T14d) confirma que un reject de `onFeeEstimated` deja `sendRawTransaction` sin llamar. El fix-pack AR-MNR-1 (`onFeeReleased`) es puramente compensatorio POST-resultado (`broadcast.ts:205-215`) — libera la reserva solo en fallos SIN spend on-chain confirmado (stale/expired), y la MANTIENE en `SPONSOR_BROADCAST_FAILED` (ambiguo, puede haber liquidado) — verificado en `broadcast.test.ts:231-283` (AR-MNR-1a/b/c). No debilita el fail-closed: el CHECK sigue siendo la única puerta de entrada al firmado.
- **opt-in-off**: confirmado. Sin `SOLANA_FEE_PAYER_SPONSOR_ENABLED=true` + key parseable → ruta no registrada, 404 real (no 501 fantasma) — `env-optin.test.ts:72-101` (T20a-d, las 4 combinaciones negativas: sin nada, flag-sin-key, key-sin-flag, key-malformada-con-flag). `isSponsorEnabled()` (`solana-fee-payer.ts:109-119`) nunca lanza — cualquier fallo de parseo cae a `false`.
- **key nunca logueada**: confirmado. `logger.ts:41-43` redacta `SOLANA_FEE_PAYER_PRIVATE_KEY`/`feePayerPrivateKey`/`SOLANA_SPONSOR_POP_SECRET`/`popProof`/`partialSignedTx`; `route.test.ts:220-241` (★T19) es una prueba empírica (no solo config) — captura el stream de logs real y el body del 502, ninguno contiene el JSON de 64 bytes de la secret key.

## EVM byte-idéntico
`git diff feat/026-wkh-205-solana-adapter...HEAD --name-only` (20 archivos totales): 4 docs SDD + `_INDEX.md` + 5 test files nuevos (`solana-sponsor.*.test.ts`) + `src/app.ts`/`src/infra/env.ts`/`src/infra/logger.ts` (aditivos, diff mostrado línea por línea, 0 líneas `-`) + 8 archivos nuevos bajo `src/methods/solana-sponsor/`, `src/core/solana-sponsor-cap.ts`, `src/infra/solana-fee-payer.ts`, `src/routes/solana-sponsor.ts`. **CERO** archivos EVM (`eip3009/*`, `wallet.ts`, `settle.ts`, `schemas.ts`, `ledger.ts`, `base-adapter.ts`, `solana-adapter.ts`) en el diff — confirmado también con un `git diff --name-only` filtrado a esos paths específicos, output vacío.

## Drift
- none — 20 archivos modificados, todos dentro del Scope IN de HU-SOL-14 (work-item.md líneas 84-99). `chain-mutex.ts` reusado sin modificación (confirmado con `git diff`, 0 cambios desde `feat/026`).

## Gates (confirmado por mí — CR/AR-report.md no persistidos en disco en este repo, contexto del orquestador usado como referencia de expectativa)
- typecheck/lint(`--max-warnings 0`)/format:check/tests: PASS — `npm run qa` corrido por QA, 933/933 verde, cadena `&&` sin cortes.
- AR: APROBADO según contexto del orquestador (drain cerrado, 1 MENOR daily-cap over-count) — el fix-pack AR-MNR-1 está presente y verificado con evidencia propia arriba (no re-tomado de fe).
- CR: APROBADO según contexto del orquestador (5 MENORes) — fix-pack CR-MNR-1/2/3/4/5 verificado con evidencia propia: MNR-3 (`broadcast.test.ts:210-228`, versioned/v0 tx), MNR-4 (`cr1.test.ts:218-227`, fee-payer no-sender), MNR-1/2/5 (tipos + `CR-MNR-1` comentarios en `broadcast.ts:132-135,257-259`, log fix `route.test.ts` comentario línea 171-173).

## Notas
- No hay `ar-report.md`/`cr-report.md` persistidos en `doc/sdd/027-hu-sol-14-gasless-feepayer/` en este repo (solo `sdd.md`, `story-HU-SOL-14.md`, `work-item.md`, `auto-blindaje.md`). Instrucción del orquestador fue no bloquear por esta ausencia y usar el resumen de gates provisto; validé el contenido sustantivo de esos gates con evidencia propia (tests citados arriba) en vez de solo confiar en el resumen.
- HELD confirmado: branch no mergeado a `main`, opt-in-off por default, devnet-only (CD-10), sin fondeo real de la wallet del fee-payer — nada de esto se tocó ni se verificó contra prod (fuera de scope de QA, founder-gated).

**Listo para DONE (HELD).**
