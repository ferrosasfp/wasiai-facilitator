/**
 * wasiai-facilitator — entry point
 *
 * NOTE: This is a scaffold placeholder. Real bootstrap logic will be
 * implemented in WFAC-2 (Core Infrastructure HU) following NexusAgil
 * pipeline (F0→DONE). DO NOT implement business logic here outside
 * that pipeline.
 */

export const VERSION = '0.1.0';

// Real bootstrap goes here in WFAC-2:
// const app = await buildApp();
// await app.listen({ port: Number(process.env.PORT ?? 3002), host: '0.0.0.0' });

console.warn('[wasiai-facilitator] Scaffold placeholder — implement via NexusAgil pipeline (WFAC-2)');
