/**
 * Global vitest setup — runs before any test file.
 * Keep minimal; per-file setup should live in the test file.
 *
 * `vitest.config.ts` already sets NODE_ENV=test and LOG_LEVEL=silent, so
 * there is nothing to do here yet. The file exists as a placeholder for
 * future HUs and as the canonical location for shared global setup.
 */

export {};
